# Kubernetes, cloud and containers.
brew "minikube"
# Required by the k/ke/kenn/kc aliases; currently present only as a minikube dependency.
brew "kubernetes-cli"
brew "dapr/tap/dapr-cli"
brew "sqldef/sqldef/psqldef"

# Backs the mysqld8 alias.
cask "rancher"
cask "lens"
cask "gcloud-cli"
